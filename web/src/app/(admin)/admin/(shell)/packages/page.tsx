import { PageHead } from '@/components/admin/PageHead';

export const metadata = { title: 'Packages' };

/** Section placeholder until F18 builds packages. The shell layout already gates the session. */
export default function PackagesPage() {
  return <PageHead title="Packages" subtitle="Arrives with F18." />;
}
