import { PageHead } from '@/components/admin/PageHead';

export const metadata = { title: 'Enquiries' };

/** Section placeholder until F21 builds enquiries. The shell layout already gates the session. */
export default function EnquiriesPage() {
  return <PageHead title="Enquiries" subtitle="Arrives with F21." />;
}
